{
  "name": "where-did-i-stop",
  "private": true,
  "description": "Static site; wrangler is here only so Workers Builds can run `npx wrangler deploy`.",
  "scripts": { "deploy": "wrangler deploy" },
  "devDependencies": { "wrangler": "^4.130.0" }
}
